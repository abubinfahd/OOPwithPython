# use of a single entity to different types in different senarios
# it referce to many forms
# example

name = "Abu Bin Fahd"
print(len(name))

name_list = ['Abu', 'Bin', 'Fahd']
print(len(name_list))

# That's polymorphism in action, a single function does know
# how to handle differents kinds of objects  as expected